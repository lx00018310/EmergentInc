# RECOVERY_REQUIRED 修复复核

## 数据重置

- 原模拟数据已移入 `workspace/recovery_backups/reset_20260916_131917/`，可以人工恢复。
- 已重建 Round 0、1 个初始 Pixel、100,000,000 初始能量；账本、预算、消息队列、Loop 历史重新初始化。
- `.env`、`glm-5.3-flash`、API 地址及 `workspace/private` 未改动。计价保留 50 元 / 2000M Token；本次没有修改计价配置。
- 原运行时创世提示词随旧数据备份，新世界提示词为空，可在前端重新设置。

## 本次确认并修复

1. **同一工作区存在两个服务实例。** 现场确认 8765、8766 同时运行。旧启动器自动换端口，使两个进程可以读写相同世界。现在使用操作系统持有的工作区锁，且不再自动更换端口；进程退出自动释放锁。
2. **运行中审计误读中间状态。** Pixel 文件、SQLite 和世界回合不是同一次写入。现在运行中明确显示 RUNNING / 审计延后；空闲时在控制器锁内进行完整审计，不将正常执行中的预留和文件同步窗口展示为恢复故障。
3. **成功响应没有落库，结算与响应状态分离。** 现在成功/格式错误响应记录、结算、消息状态在同一个 SQLite 事务内提交。失败则整体回滚；运行报告改读实际数据库及正确预算字段。
4. **后续调用失败导致已完成消息的回合信息领先世界。** 若本轮已有成功结算，异常收尾保留已发生的回合与消息队列，不冒充回滚；第一笔调用即被明确拒绝时，不空推回合。
5. **异常清理不完整。** 启动数据库失败、线程启动失败、末轮停止和快照失败均明确结束/报错；快照失败不再让控制器永久保持运行中。Loop 元数据改为原子替换写入。
6. **调用异常处理可能重复请求或捏造账目。** 移除 TypeError 后再次调用和 SDK 自动重试；使用异常类型区分明确拒绝与结果未知；缺少用量不再按预留金额虚构实际扣费。有效 usage 的 JSON 格式错误仍准确结算并结束为 ERROR。
7. **延期消息可能重复恢复。** 延期时同步清除 in_progress，避免同一消息同时存在于两个队列状态。

## 验证

- 全量自动化测试：120 passed。
- 新增隔离回归：20 轮 Run 完成后再执行 10 轮，每轮均有模拟模型调用；两次完成后审计均允许启动，调用报告与实际次数相符。
- 覆盖：后续基础设施故障、内部 TypeError 单次调用、响应写入失败时事务回滚、快照失败、启动数据库失败、延期队列恢复、缺失 usage、运行中审计延后、工作区锁竞争及释放。
- 正式服务只运行 8765；实际尝试从另一个进程启动 8766，得到 WORKSPACE_IN_USE，未打开第二个服务。
- 重建后 API 实测：Round 0、1 Pixel、账本差额 0、OPEN 预留 0、未知调用 0、卡死 Loop 0、allowed_to_start=true。
- 未调用真实模型进行长跑，也未消耗新世界的回合；测试使用隔离目录和模拟提供方。

## 不能承诺的边界

这次消除了上述已确认的软件缺陷，不等于“任何情况下永不需要恢复”。断电、磁盘损坏、供应商处理后断线或返回不完整用量，仍可能需要人工核对。遇到真实未知计费不能自动退款、重试或绕过审计；跨文件/外部工具副作用也不是完整的数据库事务。本次没有将这些真实故障伪装成成功。
