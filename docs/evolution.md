# Evolution

Spawn 必须由父 Pixel LLM 主动提出。

子代：

```text
Child Genome = Parent Genome + exactly one mutation
```

单次允许：
- 一个 tendency 的小幅变化
- 增加一条 learned principle
- 删除一条 learned principle
- 改写一条 strategy sentence

不能一次重写整个 genome。

子代不继承父代完整 memory/history/current problem。
